Add unDraw illustrations here (download as SVG from `https://undraw.co/illustrations`).

Recommended usage:
- Keep file names stable (e.g. `no_clinics.svg`, `select_clinic.svg`).
- Prefer SVG for crisp scaling.
- You can recolor on unDraw to match the app's primary color before downloading.



